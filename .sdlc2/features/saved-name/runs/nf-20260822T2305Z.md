# sdlc2 run report — saved-name

**Feature:** saved-name — "During a visit I want to save the name I was greeted as, so I can be
greeted that way again without retyping it"
**Run:** nf-20260822T2305Z
**Base branch:** main

## 1. Node results

| node | verdict | score | rounds | arbitrated | VH ids | reason |
| --- | --- | --- | --- | --- | --- | --- |
| po | pass | 0.88 | 2 | no | — | — |
| architect | pass | 0.87 | 2 | no | — | — |
| ux | soft-pass | 0.79 | 2 | yes | VH-ux-01, VH-ux-02 | — |
| build | pass | — | 0 | no | — | — |

Per-round detail for every node that used more than half its rounds:

- **po:** 0.72 [10 defects, 1 open] → 0.88 [6 defects, 0 open] — climbing. Converged and passed
  inside its round budget.
- **architect:** — [round 1: maker output rejected outright, 1 defect, 1 open, no score] → 0.87
  [4 defects, 0 open] — round 1 produced nothing to score; round 2 recovered and passed cleanly.
- **ux:** 0.79 [11 defects, 0 open] → — [round 2: maker output rejected, 1 defect, 1 open, no
  score] — this is not a climb: the second round dropped to no score after the maker's output was
  rejected, and the loop did not resolve on its own. It was arbitrated to soft-pass rather than
  cleared by a maker/checker round — see VH-ux-01, which reads the round-2 rejection as an
  engine/transport failure rather than a content defect, and VH-ux-02.

## 2. Slices

| slice | attempts | verdict | branch@sha | cut from | review | reason |
| --- | --- | --- | --- | --- | --- | --- |
| 01-save-the-greeted-name | 1 | pass | slice/saved-name/01-save-the-greeted-name@ea96f83 | main | 0.95 | — |
| 02-greet-again-as-the-saved-name | 1 | pass | slice/saved-name/02-greet-again-as-the-saved-name@e6ab966 | slice/saved-name/01-save-the-greeted-name (stacked on the blocker it declared) | 0.94 | — |
| 03-be-reminded-of-the-saved-name | 1 | pass | slice/saved-name/03-be-reminded-of-the-saved-name@0fd1266 | slice/saved-name/01-save-the-greeted-name (stacked on the blocker it declared) | 0.96 | — |
| 04-replace-the-saved-name | 1 | pass | slice/saved-name/04-replace-the-saved-name@3c3b14c | slice/saved-name/01-save-the-greeted-name (stacked on the blocker it declared) | 0.94 | — |
| 05-fresh-visit-clears-the-saved-name | 3 | pass | slice/saved-name/05-fresh-visit-clears-the-saved-name@87b12f2 | slice/saved-name/03-be-reminded-of-the-saved-name (stacked on the blocker it declared) | 0.95 | — |

All five slices passed; note slice 05 needed 3 attempts before passing, the only slice that did
not pass on its first try.

## 3. Human-verify index (OPEN rows in VERIFY-WITH-HUMAN.md)

| id | node | severity | one-line decision |
| --- | --- | --- | --- |
| VH-01 | po | unconfirmed (no severity stated) | Region text on save reuses the hint's own string, "Saved: <name>"; po-authored copy, not yet confirmed by a human. |
| VH-02 | ux | unspecified — screen-reader check | Ships the testable half (attribute present, text correct) now; flags 6 named audible re-announcement checks (a)-(f) to run with a real screen reader once issues 01 and 04 are built. |
| VH-03 | architect | unspecified | Issue 04 needs slice 02's "Greet me again" control for one assertion; rather than edit the po node's issue file, the architect declared slice 04 blocked by 01 **and** 02 in design.md/ADR-0025 — needs a human to confirm this queue edge (or prefer the po node amend the issue instead). |
| VH-04 | ux | unspecified — screen-reader check | Sub-check of VH-02: on the very first save, the live region's re-render also inserts "Greet me again" as a new child; VH-02(f) asks a human to record what is actually announced on that one transition. |
| VH-ux-01 | ux | critical | Round-2 checker's sole defect ("maker agent returned nothing") is read as an engine/transport failure, not a content defect — the round-2 `mockup.html` is kept as final, with four additive self-review corrections (variant labels, a missing state-matrix row, cross-reference fixes, focus documented). |
| VH-ux-02 | ux | minor | Focus after "Greet me again" is unspecified by any acceptance criterion; the `ux` node fixed it to match the save button's rule — focus stays on the activated control, no `focus()` call anywhere — and asks a human to confirm via VH-ux-02(a) during the same screen-reader pass. |

## 4. Maker disputes

- **architect / criterion "engine":** *"Not disputed on substance — the changelog was genuinely
  26 lines and that is now fixed. Noted only so the record is clear: the defect was about my
  response envelope, not about design.md or any ADR, so no artifact content was changed this
  round and nothing that passed in round 1 could regress."*

## 5. Summary

**This run soft-passed at the `ux` node** — it is not clean. `po`, `architect` and `build` all
passed; `ux` reached soft-pass only after arbitration when its round-2 maker output was rejected
outright, leaving two open VH decisions (VH-ux-01, VH-ux-02) alongside four earlier open items
(VH-01 through VH-04). All five slices built and passed review, with slice 05 needing three
attempts and slices 02/03/04 stacked on slice 01's branch (04 further requires 02, per VH-03).

**Next human action:** work the human-verify index above — most urgently the screen-reader passes
under VH-02/VH-04/VH-ux-02(a), and confirmation of the po-proposed copy (VH-01) and the slice-04
queue edge (VH-03) — then review and merge the `slice/saved-name/*` branches. sdlc2 has not merged
anything itself; reviewing and merging those branches is the human's job.
